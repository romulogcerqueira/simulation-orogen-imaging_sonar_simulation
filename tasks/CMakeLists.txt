# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(gpu_sonar_simulationTaskLib)
ADD_LIBRARY(${GPU_SONAR_SIMULATION_TASKLIB_NAME} SHARED 
    ${GPU_SONAR_SIMULATION_TASKLIB_SOURCES})
add_dependencies(${GPU_SONAR_SIMULATION_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${GPU_SONAR_SIMULATION_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${GPU_SONAR_SIMULATION_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${GPU_SONAR_SIMULATION_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${GPU_SONAR_SIMULATION_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${GPU_SONAR_SIMULATION_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${GPU_SONAR_SIMULATION_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${GPU_SONAR_SIMULATION_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${GPU_SONAR_SIMULATION_TASKLIB_HEADERS}
    DESTINATION include/orocos/gpu_sonar_simulation)

