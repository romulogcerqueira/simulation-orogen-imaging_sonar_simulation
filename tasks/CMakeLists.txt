# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(imaging_sonar_simulationTaskLib)
ADD_LIBRARY(${IMAGING_SONAR_SIMULATION_TASKLIB_NAME} SHARED
    ${IMAGING_SONAR_SIMULATION_TASKLIB_SOURCES})
add_dependencies(${IMAGING_SONAR_SIMULATION_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${IMAGING_SONAR_SIMULATION_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${IMAGING_SONAR_SIMULATION_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${IMAGING_SONAR_SIMULATION_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${IMAGING_SONAR_SIMULATION_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${IMAGING_SONAR_SIMULATION_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${IMAGING_SONAR_SIMULATION_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${IMAGING_SONAR_SIMULATION_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${IMAGING_SONAR_SIMULATION_TASKLIB_HEADERS}
    DESTINATION include/orocos/imaging_sonar_simulation)

