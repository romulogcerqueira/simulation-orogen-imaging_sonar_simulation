PROJECT(gpu_sonar_simulation)
cmake_minimum_required(VERSION 2.6)

SET (CMAKE_MODULE_PATH "${CMAKE_SOURCE_DIR}/.orogen/config")
INCLUDE(gpu_sonar_simulationBase)

# FIND_PACKAGE(KDL)
# FIND_PACKAGE(OCL)

